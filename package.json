{
  "name": "terminar",
  "version": "0.6.0",
  "description": "terminar - Persistent terminal sessions (Electron + Rust server)",
  "scripts": {
    "server:build": "cd server && PATH=\"$HOME/.cargo/bin:$PATH\" cargo build --release && ([ \"$(uname)\" = Darwin ] && codesign --force --sign - target/release/terminar-server 2>/dev/null; true)",
    "server:dev": "cd server && PATH=\"$HOME/.cargo/bin:$PATH\" cargo build --bin terminar-server && ([ \"$(uname)\" = Darwin ] && codesign --force --sign - target/debug/terminar-server 2>/dev/null; true) && PATH=\"$HOME/.cargo/bin:$PATH\" ./target/debug/terminar-server --no-auth --port 6750",
    "server:stop": "node -e \"require('./npm/terminar/lib/server-manager.js').stop()\"",
    "server:status": "node -e \"require('./npm/terminar/lib/server-manager.js').status()\"",
    "server:logs": "node -e \"require('./npm/terminar/lib/server-manager.js').logs()\"",
    "server:restart": "node -e \"const s=require('./npm/terminar/lib/server-manager.js');const p=require('path');s.restart(p.resolve('server/target/debug/terminar-server'),6750)\"",
    "web:dev": "cd web && pnpm dev",
    "tray:dev": "cd tray && pnpm dev",
    "tray:build": "cd tray && pnpm build:electron",
    "dev": "node scripts/dev.cjs tray",
    "dev:web": "node scripts/dev.cjs web",
    "dev:all": "node scripts/dev.cjs web tray",
    "build": "pnpm server:build && cd tray && pnpm build",
    "test": "cd server && PATH=\"$HOME/.cargo/bin:$PATH\" cargo test",
    "test:server": "cd server && PATH=\"$HOME/.cargo/bin:$PATH\" cargo test",
    "test:web": "cd web && pnpm test",
    "test:extension": "cd extension && pnpm test",
    "tray:test": "cd tray && pnpm test"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild",
      "node-pty"
    ]
  }
}
